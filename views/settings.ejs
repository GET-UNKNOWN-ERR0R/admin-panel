<!DOCTYPE html>
<html>

<head>
    <title>Settings</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>

    <nav class="nav">
        <h2>Settings</h2>
        <a href="/admin/dashboard">Dashboard</a>
    </nav>

    <div class="container">

        <div class="card">
            <h2>About Section Settings</h2>

            <form method="POST" enctype="multipart/form-data" class="settings-form">

                <label>About Line 1</label>
                <input type="text" name="aboutLine1" value="<%= settings?.aboutLine1 || '' %>"
                    placeholder="About line 1" required />

                <label>About Line 2</label>
                <input type="text" name="aboutLine2" value="<%= settings?.aboutLine2 || '' %>"
                    placeholder="About line 2" required />

                <input type="hidden" name="oldProfile" value="<%= settings?.profileImage || '' %>" />
                <input type="hidden" name="oldResume" value="<%= settings?.resumeLink || '' %>" />


                <label>Profile Image</label>
                <input type="file" name="profileImage" />

                <% if(settings?.profileImage){ %>
                    <div class="preview-box">
                        <p>Current Profile Image:</p>
                        <img src="<%= settings.profileImage %>" alt="Profile" />
                    </div>
                    <% } %>

                        <label>Resume URL</label>
                        <input type="url" name="resumeLink" id="resumeInput" placeholder="https://drive.google.com/..."
                            value="<%= settings?.resumeLink || '' %>" oninput="updateResumePreview()" required />

                        <button type="submit">Update Settings</button>
            </form>
        </div>

    </div>

</body>

</html>